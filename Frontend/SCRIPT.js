const button = document.getElementById('search-button'); //From html document where we assigened an id, get the id and assign to a variable
button.addEventListener("click", function() { //add an event listener to the button, when clicked, it will run the function

})
button.addEventListener("mouseover", function() { //add an event listener to the button, when mouse is over it, it will run the function
    button.style.; //make it slightly bigger
}